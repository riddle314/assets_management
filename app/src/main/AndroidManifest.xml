<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assetmanagement">

    <application
        android:name=".AssetManagementApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AssetManagement">
        <activity
            android:name="com.example.assetmanagement.usecases.transactions_activity.TransactionsActivity"
            android:exported="true"
            android:label="@string/title_activity_transactions"
            android:theme="@style/Theme.AssetManagement">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".usecases.MainActivity" />
    </application>

</manifest>